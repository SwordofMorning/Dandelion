# Agent 缓存命中率分析（修正版）——排序策略有效性与真实失配原因

> 分析对象：`sess_20260728_145246_283286`（2026-07-28 ~ 08-03）
> 数据来源：`api.log`（118 段：PRE/POST 严格交替、时间单调）、`history.log`、`src/` 源码、DeepSeek 平台统计
> 本文档为修正版：初版曾断言"system prompt 时间戳是命中率低的头号原因"，经严格配对分析后被推翻，详见第五章。

---

## 一、TL;DR（结论先行）

1. **你的"tools 在前、system 在后"排序策略是有效的**。证据：system 时间戳变化（跨分钟）的请求中，仍有 11/28（39%）实现了接近 100% 的命中（例如 idx26 只 miss 249 token ≈ system 自身大小，tools+messages 前缀全部命中）——若 system 位于缓存前缀最前，这些请求必然全 miss。
2. **命中/失配是二元事件**：36 个"全 miss"请求贡献了全部 miss tokens 的 97%。其余请求几乎都是 95%+ 命中。
3. **全 miss 与 system 时间戳零相关**：36 个全 miss 请求中，system 完全相同的（同分钟）17 个、system 变化的（跨分钟）17 个——各占一半。甚至存在"system/tools/messages 字节级完全一致却全 miss"的请求（idx25），证明相当一部分 miss 来自**客户端无法观测的服务端因素**。
4. 真实的可控失配因素（按影响排序）：**上下文截断（5 次，命中率 51.5%）> 跨天冷启动（2 次）> 模型切换（pro→flash 1 次）> 周期性全 miss（服务端因素，~34 次）**。
5. 时间戳优化仍建议做（降频无害、可减少 system 段重算、为将来迁 Anthropic 显式断点做准备），但**它不是当前 75% 命中率的主要瓶颈**。

---

## 二、数据事实（严格配对后）

| 观测项 | 数值 | 说明 |
| --- | --- | --- |
| 请求总数 | 118 | 7/28（13 个）+ 7/30（2 个）+ 8/3（~103 个） |
| 全会话命中率（日志口径） | 61.6% | read=2,973,184 / (read 2,973,184 + miss 1,855,242) |
| 全会话命中率（平台口径） | 75.1% | 2,238,976 / 2,979,243（平台统计口径与日志 usage 字段有差异） |
| 同分钟（system 相同）命中率 | 70.1% | 70 对 |
| 跨分钟（system 变化）命中率 | 46.4% | 28 对 |
| 截断后请求命中率 | 51.5% | 5 对 |
| **全 miss 请求数** | **36/118** | **贡献 miss tokens 的 97%** |
| 全 miss 中：同分钟 / 跨分钟 | 17 / 17 | **与 system 变化零相关** |
| 相邻请求消息前缀稳定率 | 100%（非截断时） | 严格追加模式 |

**关键推论**：
- 命中率 ≈ 1 -（全 miss 请求的 token 占比）。优化缓存 = 降低"全 miss 事件"的频率和单次规模。
- system 时间戳变化**不会击穿** tools+messages 前缀（否则跨分钟请求应 100% 全 miss，实际仅 ~61% 全 miss，且存在仅 miss 249 token 的部分命中案例）。

---

## 三、请求结构与你的排序策略

你的请求结构（JSON 字段顺序）：

```
tools（13 个，稳定不变）
messages（严格追加的历史）
max_tokens
system（含分钟级时间戳 + 技能目录 + 记忆，可能变化）
```

### 3.1 你的策略为什么有效（证据链）

**证据 A——"system 变了却几乎全命中"（idx25 → idx26）**：

| 请求 | system 时间 | tools | messages | read | miss |
| --- | --- | --- | --- | --- | --- |
| idx25 (10:08:49) | 10:08 AM | 相同 | 43 条 | 0 | 46984 |
| idx26 (10:10:16) | **10:10 AM** | 相同 | 45 条（追加 2） | **46976** | **249** |

idx26 的 system 与 idx25 完全不同（diff 显示 `'8' → '10'`），却命中了 idx25 建立的缓存 46976 tokens（≈ idx25 的全部输入 46984），miss 仅 249（≈ system 段自身大小）。**若 system 位于缓存前缀最前，idx26 命中率应为 0%。**

**证据 B——"system 只差 1 个字符却全 miss"（idx18 → idx19）**：

| 请求 | system 时间 | read | miss |
| --- | --- | --- | --- |
| idx18 (10:02:56) | 10:02 AM | 6528 | 2265 |
| idx19 (10:03:00) | **10:03 AM**（diff 仅 `'2'→'3'`） | 0 | 14750 |

system 仅差 1 个字符（分钟数字）却全 miss——如果 system 在末尾，这 1 字符只应造成 ~250 token 的 miss。**说明 1 字符差异不是 idx19 全 miss 的原因，而是另有服务端因素**（idx19 之前 idx15-18 连续命中，idx19 恰好落入"周期性全 miss"）。

**证据 C——"前缀字节级一致却全 miss"（idx24 → idx25）**：system 完全相同（hash 一致）、tools 完全相同、messages 前 41 条完全一致（严格追加），idx25 却 read=0 全 miss。**这只能用服务端因素解释**（节点分片、缓存写入异步/未就绪、缓存驱逐）。

**证据 D——全 miss 分布与跨分钟零相关**：36 个全 miss 请求，同分钟 17 / 跨分钟 17。若 system 变化导致全 miss，跨分钟应远多于同分钟。

### 3.2 结论

- 你的排序策略**有效**：system 位于请求最末，其变化只影响 system 段自身（~250-500 token），不击穿 tools+messages 前缀。
- 这一点与 DeepSeek 的缓存机制自洽：DeepSeek 按 **64-token 存储单元**做块级前缀匹配；system 段在末尾时，其变化只导致末尾块的失配。
- 若把 system 移到 tools 之前（或 tools 之后 messages 之前），时间戳变化将影响更长的前缀——**保持现状，不要改回**。

---

## 四、命中率基准对比

| 基准 | 命中率 | 说明 |
| --- | --- | --- |
| 无缓存优化的典型 agent 循环 | <20% | 每轮重排/重写/注入时间戳，前缀全失配 |
| **你的 Regent（平台口径）** | **75.1%** | 排序策略有效 + 消息严格追加 |
| 你的 Regent（日志口径） | 61.6% | 含 7/28 失败请求等，口径更严格 |
| 理想稳定前缀 | 99.8% | DeepSeek 官方/第三方实测 |
| Claude Code 会话 | 90~96% | 显式断点 + 5 分钟 TTL 高频续期 |

**位置判定**：你的命中率远高于"无优化 agent"（<20%），核心优势正是"不变内容前置、可变内容后置"的排序策略 + 消息严格追加。差距来自第四节列出的可控/不可控因素，而非 system 位置。

---

## 五、真实失配原因分解（按影响排序）

### 原因 1：周期性"全 miss"事件（服务端因素为主，~34 次，影响最大）

- **现象**：每运行几个请求，会出现 1~3 个全 miss 请求（read=0），随后紧跟的请求必然高命中（read ≈ 前一个 miss，如 46984 → 46976、54084 → 54400）。
- **证据**：idx15, 19, 23, 24, 25, 27, 32, 35, 41, 45, 49, 57, 62, 65, 66, 76, 78, 89, 92, 96, 101, 103, 104, 106, 107, 108 等；其中 idx25 前缀与前一请求字节级一致仍全 miss。
- **可能机制**：DeepSeek 分布式部署的节点路由分片（缓存按节点隔离，切换节点即 miss）、缓存写入异步生效（"全 miss 请求写入缓存、下一请求命中"的模式）、或缓存驱逐。客户端无法直接观测，只能通过"read ≈ 前一 miss"的指纹识别。
- **客户端可做的**：无直接手段；但可通过"全 miss 后下一请求必然命中"的规律预判成本节奏。

### 原因 2：上下文截断（5 次，命中率 51.5%）

- **证据**：idx53（81→41 条）、idx59（51→19 条）、idx82（63→33 条）、idx110（63→33 条）等；代码 `agent.py:_compact_context()`（>40 条消息触发，保留 head 5 + snip + tail）。
- **影响**：截断后 messages 前缀大变，中段缓存全部作废；截断后首个请求全 miss（idx53 miss=27263、idx110 miss=81048）。
- **改进**：增大保留窗口 / 用摘要替代删除 / 截断前将关键上下文写入文件供后续读取。

### 原因 3：跨天冷启动（2 次，单次大）

- **证据**：idx13（7/30 首个，miss=3388）、idx15（8/3 首个，miss=4390）。DeepSeek 缓存数小时~数天自动清理。
- **改进**：长会话避免跨天续用；任务间隔超过数小时建议新开 session。

### 原因 4：模型切换（1 次，pro→flash）

- **证据**：idx55 从 deepseek-v4-pro 切换为 deepseek-v4-flash，read=0 全 miss（且 flash 阶段初期 idx57 也全 miss）。
- **机制**：缓存按模型隔离。
- **改进**：同一会话固定模型；fallback 降级只接受单次 miss，避免高频抖动。

### 原因 5：system 时间戳变化（存在但影响有限）

- **数据**：跨分钟命中率 46.4% vs 同分钟 70.1%——存在约 24 个百分点的差距，但该差距主要来自"全 miss 事件在跨分钟段中的分布"，而非 system 直接击穿前缀（见证据 A/D）。
- **影响**：system 段自身 ~250-500 token 的每次重算（可忽略）；真正的隐性成本是"任何 system 变化都会使整个 system 段无法复用缓存"——在 Anthropic 显式断点模型下（5 分钟 TTL）影响会被放大。
- **改进（低优先级但建议做）**：时间戳降为会话级固定或小时级，为将来迁移 Anthropic 做准备。

---

## 六、与 Claude Code / Codex 的差距

| 维度 | Claude Code | Codex | Regent（现状） | 判定 |
| --- | --- | --- | --- | --- |
| 缓存控制 | 显式 cache_control 断点（最多 4 个） | 自动前缀缓存 | 依赖 DeepSeek 自动缓存 | 可接受（DeepSeek 不支持断点） |
| 内容排序 | 稳定内容（system/tools/CLAUDE.md）前置 + 断点 | 稳定前缀 | **tools 前置、system 后置** | **已达标，策略正确** |
| 动态内容位置 | 断点之后 | 尾部 | 尾部（system 在末尾） | **已达标** |
| 消息前缀 | 严格追加 | 严格追加 | 100% 严格追加（非截断时） | **已达标** |
| 上下文压缩 | 摘要式 compact | 类似 | 截断式 snip | 中档差距 |
| 会话生命周期 | 短会话连续操作 | 短会话 | 跨天长会话 | 中档差距 |
| 模型路由 | 稳定 | 稳定 | flash/pro 混用（1 次切换） | 轻微差距 |
| 缓存度量 | 有 | 有 | 日志有 usage 无报表 | 需要补 |

**一句话**：你的系统在"内容排序、动态内容下沉、消息前缀"三个缓存核心维度上**已经达到业界水准**；差距集中在"压缩策略、会话生命周期、模型路由、度量报表"四个工程化习惯。

---

## 七、改进路线图（按性价比）

### P0：保持现有排序策略，勿回退

- 维持"tools → messages → system"的字段顺序；**不要**把 system 移到请求前部。
- 若未来引入显式 cache_control（Anthropic），把断点放在 tools 之后、system 之前。

### P1：降低可观测失配

1. **上下文压缩优化**：`_compact_context()` 保留窗口从 5 条扩大（如 8-10 条），或对中段做摘要而非删除；记录"截断后首个请求"的 miss 到日志。
2. **会话生命周期**：任务间隔 > 数小时时新开 session；单会话保持连续操作节奏。
3. **固定模型路由**：同一会话避免 flash/pro 混用；fallback 只允许一次性降级。

### P2：度量与预防

4. **缓存度量报表**：在 `safe_llm.py`/logger 中按请求记录 read/miss（DeepSeek 兼容端点不填充 `cache_creation_input_tokens`，从平台 `prompt_cache_hit/miss_tokens` 或响应原始字段取），会话结束输出：命中率、全 miss 事件数、截断次数、模型切换次数。
5. **时间戳降频**（可选）：会话级固定时间，消除跨分钟 system 段重算，并为 Anthropic 迁移做准备。

### 预期收益

- 消除截断 + 固定模型 + 会话管理：全 miss 事件从 36 次降至 ~20 次（服务端因素为主的部分无法消除），命中率预计 75% → 85%+。
- 时间戳降频：额外 2~5 个百分点（system 段重算减少 + 未来兼容性）。

---

## 八、Agent 开发通用原则（修正版）

### 原则 1：请求 = 稳定的前缀 + 增长的尾部（你的策略正是此原则的实践）

- 工具定义、消息历史 = 稳定前置；
- 动态内容（时间戳、技能目录、记忆）= 后置；
- 服务端缓存（DeepSeek 按 64-token 块）对前缀做匹配，尾部变化代价最小。

### 原则 2：命中是二元事件，全 miss 是主要成本

- 观察显示：一旦全 miss，整段前缀全部重算；全 miss 请求贡献 97% 的 miss tokens。
- 因此优化目标 = 降低"全 miss 事件"频率（截断、切模型、冷启动、长间隔），而非纠结于单 token 的字节差异。

### 原则 3：区分"客户端可控"与"服务端因素"

- 前缀字节级一致仍可能全 miss（idx25）——这是服务端节点/异步因素，客户端日志无法观测。
- 判断方法：若"内容一致却 miss"，且下一个请求必然命中（read ≈ 前一 miss），则属于服务端写入生效延迟/节点路由。
- 不要为无法控制的因素过度优化；聚焦截断、模型、会话生命周期。

### 原则 4：压缩要保留前缀

- 保留 head（前缀）+ 尾部（近期对话），中间摘要/占位；全量清空 = 下次请求全量 miss。

### 原则 5：模型是缓存的 key

- 模型切换 = 缓存切换；路由策略要意识到单次切换的代价（全 miss 一次）。

### 原则 6：先度量，再优化

- 每次改动前后对比：命中率、全 miss 事件数、截断次数、模型切换次数。没有数字的优化是玄学。

---

## 九、验证方法（可复现）

```python
# 1. 按 PRE/POST 严格交替配对（本日志 118 段，时间单调，无并发交错）
# 2. 对相邻请求对计算 system 差异（diff 定位到字符级：idx18->19 仅 '2'->'3'）
# 3. 分类统计：SAME分钟 / DIFF分钟 / TRUNC 的 read/miss 分布
# 4. 识别全 miss 事件（read=0）并检查其与 system 变化的相关性（17/17 对半分）
# 5. 用"read ≈ 前一请求 miss"的指纹识别服务端写入生效延迟
```

（分析脚本：`/tmp/cache_analysis.py`，可复用于后续会话。）
